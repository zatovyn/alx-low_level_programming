#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char helloputs[] = "\"Programming is like building a multilingual puzzle";

	puts(helloputs);
	return (0);
}
